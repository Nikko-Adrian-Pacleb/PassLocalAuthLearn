<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>
  <body>
    <h1>Home Page</h1>
    <h2>User: <%= user.username %></h5>
    <br />
    <h3>Notes:</h6>
    <% if(notes.length === 0){ %>
    <h4>No notes found</h4>
    <% } else {%>
        <ul>
      <% notes.forEach((note) => { %>
      <li><%= note.contentPreview %></li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
